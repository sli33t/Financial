server:
  port: 9001

spring:
  application:
    name: financial-user
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/code1_2?characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&serverTimezone=UTC
    username: root
    password: root
  redis:
    password:
    host: localhost
    port: 6379

#转化为小驼峰
mybatis:
  configuration:
    map-underscore-to-camel-case: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/
